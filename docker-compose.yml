services:
  app:
    build:
      args:
        user: akuma
        uid: 1000
      context: ./
      dockerfile: Dockerfile
    image: perpustakaan-app
    container_name: perpustakaan-app
    restart: unless-stopped
    working_dir: /var/www/
    volumes:
      - ./:/var/www
    networks:
      - perpustakaan
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3

  webserver:
    image: nginx:alpine
    container_name: perpustakaan-webserver
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./:/var/www
      - ./docker-compose/nginx/perpustakaan.conf:/etc/nginx/conf.d/default.conf
    networks:
      - perpustakaan
    depends_on:
      - app

  db:
    image: mysql:8.0
    container_name: perpustakaan-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-perpustakaan}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-secret}
      MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
      MYSQL_USER: ${DB_USERNAME:-perpustakaan}
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - perpustakaan
    ports:
      - "3306:3306"

networks:
  perpustakaan:
    driver: bridge

volumes:
  dbdata:
    driver: local
